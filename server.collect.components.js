const modules = require("./server/app/modules-index/module-index");
const middlewares = require("./server/middlewares/middlewares-index/middlewares-index");
const routers = require("./server/api/routers/routers-index");

module.exports = {modules, middlewares, routers}